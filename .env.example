# Gemeinsame Umgebungsvariablen für API + Frontend (Docker)

# API
PORT=3000
NODE_ENV=production
ZIP_CODES=68159,68161,68309,68519
CACHE_TTL_MINUTES=60
REFRESH_CRON=0 */6 * * *
API_RATE_LIMIT_PER_MINUTE=60

# Frontend
FRONTEND_PORT=8080
